const { check } = require('express-validator');
const validatorMiddleware = require('../../app/middlewares/validatorMiddleware');

/* exports.getTaskByIdVali = [
     check('username').isMongoId().withMessage('ahmed hamed') ,
    validatorMiddleware 
];*/
